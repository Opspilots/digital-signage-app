name: Deploy digital-signage-app
on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-digital-signage
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Rsync source (excl. uploads / node_modules / .env / persisted data / build output / graphify cache)
        run: |
          rsync -avz --delete \
            --exclude 'uploads/' --exclude 'node_modules/' \
            --exclude '.git' --exclude '.github' --exclude '.env' \
            --exclude 'data/' --exclude 'backend/data/' \
            --exclude 'backend/dist/' --exclude 'frontend/dist/' \
            --exclude 'graphify-out/' \
            -e "ssh -i ~/.ssh/id_ed25519" \
            ./ ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/var/www/digital-signage-app/

      - name: Rebuild & restart
        run: |
          ssh -i ~/.ssh/id_ed25519 ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} '
            set -e
            cd /var/www/digital-signage-app
            (cd backend && npm install --production=false && npm run build)
            (cd frontend && npm install --production=false && npm run build)
            sudo /usr/bin/systemctl restart digital-signage.service
          '